# ============================================
# Mastra S3 Vector RAG - Environment Variables
# ============================================

# --------------------------------------------
# Runtime Configuration (ローカル開発・本番共通)
# --------------------------------------------

# Mastra JWT認証シークレット（必須）
# 本番環境では強力なランダム文字列を使用してください
MASTRA_JWT_SECRET=your-jwt-secret-here

# AWS認証情報（ローカル開発時）
# 本番環境（Lambda）ではIAMロールを使用するため不要
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS認証プロファイル（オプション）
# AWS CLIプロファイルを使用する場合は指定
# AWS_PROFILE=your-profile-name

# AWSリージョン（デフォルト: ap-southeast-2）
# S3 Vectors対応リージョン: us-east-1, us-east-2, us-west-2, eu-central-1, ap-southeast-2
AWS_REGION=ap-southeast-2

# S3 Vectorsバケット名（必須）
S3_VECTORS_BUCKET_NAME=your-s3vectors-bucket

# S3 Vectorsインデックス名（オプション、デフォルト: knowledge）
S3_VECTORS_INDEX_NAME=knowledge

# --------------------------------------------
# CDK Deployment Configuration (デプロイ時のみ)
# --------------------------------------------

# AWSアカウントID（CDKデプロイ時必須）
# 取得方法: aws sts get-caller-identity --query Account --output text
CDK_DEFAULT_ACCOUNT=123456789012

# CDKデプロイ先リージョン（CDKデプロイ時必須）
CDK_DEFAULT_REGION=ap-southeast-2

# S3 VectorsバケットのARN（CDKデプロイ時必須）
# 形式: arn:aws:s3vectors:REGION:ACCOUNT:bucket/BUCKET_NAME
S3_VECTORS_BUCKET_ARN=arn:aws:s3vectors:ap-southeast-2:123456789012:bucket/your-s3vectors-bucket

# Node.js環境（本番デプロイ時推奨: production）
NODE_ENV=development
